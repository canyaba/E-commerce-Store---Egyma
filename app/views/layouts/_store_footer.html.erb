<footer class="border-top bg-white mt-5">
  <div class="container py-4 d-flex flex-column flex-md-row justify-content-between gap-3">
    <div>
      <p class="mb-1 fw-semibold">Egyma</p>
      <p class="mb-0 text-muted">Digital fitness resources for structured training, recovery, and nutrition.</p>
    </div>
    <div class="text-md-end">
      <p class="mb-1 fw-semibold">Browse by category</p>
      <p class="mb-0 text-muted"><%= storefront_categories.map(&:name).join(' • ') %></p>
    </div>
  </div>
</footer>
